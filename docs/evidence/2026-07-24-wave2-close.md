# composable-orchestration-v2（Wave 2）完了証拠: 受入matrix・gate実績・知識還流

## 受入matrix（Task別）

| Task | 実施 | 受入 | 備考 |
|---|---|---|---|
| w2-external-source-binding (fm-0665) | manifest v30: Task `external_source` closed tuple・digest null正規化（admission/packet両方）・v29↔v30 migration/rollback・SCHEMA_UPGRADE_REQUIRED gate | 親直轄（F）。最上位refuterの設計反証5指摘を全採用したADR 0116に従い実装。focused test 5本追加 | packet_digest正規化はrefuter指摘1（走行中worker跨ぎmigrationのreport回収破壊）の直接対処 |
| w2-lattice-readonly-projection (fm-0666) | lib/orchestrate/lattice-projection.mjs 新設。stdout優先判別・project_state 4値・exact schema・digest形式検証＋透過 | aiterm Codex（gpt-5.6-terra×medium）委譲、Worker Run記録・受入差分1往復（digest再計算照合の除去）の上で親accept | 受入詳細は docs/evidence/2026-07-24-wave2-projection-acceptance.md |

## gate実績（Wave 2完了時点）

- `node --test tests/orchestrate/*.test.mjs`: 全green（v30系5本・projection 7本を含む）
- `make lint`: pass
- 既存全Control（v25〜v29・24件）の読取互換: v30追加後もstatus/manifest走査が全件green（task-record duplicate横断検査が全manifest読込を通ることで実証）

## 手続実績と逸脱

- Control lifecycle実走: v1のbudget null上限欠陥をADR 0117の継続裁定で回復（cancel→finalize→archive→v2 successor init）。placement-dry-run→reserve（capacity review decision付き）→admit→packet→dispatch→受入まで初の完全実走。
- 逸脱1件: 親がcompleted観測をreport importより先に記録し、strict Worker Report importが構造的に不能となった。acceptのdecision_noteへ記録し、罠DBへ再発防止を記録済み（worker-report-import順序）。

## 知識還流

- ADR 0116（設計と反証裁定）・ADR 0117（v1閉鎖とv2継続）
- caveat: Control init budget null上限の恒久ブロック／worker-report-import順序罠（いずれもprivate）
- shared/orchestrate/control-record.md へ external_source 契約・migration・digest正規化を正典化

## Wave 3への引き渡し

- Latticeにper-task公開digestが無い事実（ADR 0116 Decision 6）は fm-0667 のcharacterize入力
- Wave 3のControl は composable-orchestration-v3 として successor init する（v2はWave 2で閉じる）
