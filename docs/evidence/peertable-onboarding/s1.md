# s1 診断契約の設計とroom合意 — 完了証跡

- 契約は2026-08-08の円卓（つむぎ・ひなた・こはる・親bell）で全員合意し、peertable repo `docs/plan.md` **決定45**として正本化された（peertable commit `24f4409`）。
- 合意内容: 入口 `peertable-client diagnostics --json`、schema `peertable.native_factory_diagnostics.v1`、checks = version_consistency / bin_integrity / node_runtime / skill_bundle / room_reachability、`PEERTABLE_URL`未設定は`not_applicable`、unverified＝check実行自体がI/Oエラーで判定不能の時だけ、exit code は overall=ready だけ 0。
- 2026-08-10 実測（peertable 0.3.5）: URL無し実行で `{"overall":"ready","checks":{...,"room_reachability":"not_applicable"}}`・exit 0、URL有りで `room_reachability: pass`。契約と実装の一致を確認。
- 記録者: bell（親）。circle中断（2026-08-08）により当時close未達だったものを、再開時照合でdoneへ記録する。
