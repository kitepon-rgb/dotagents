# GF07 — Claude・Codex無退行と工場Grok受入

- 受入日: 2026-08-14
- Lattice: `grok-factory-application` / `GF07` / sequence 17
- 親裁定: room #122、provider smoke結果: room #131
- 変更repo: Spotter、Throughline
- 非変更repo: aiterm-mcpを含むその他の工場製品
- 非実施: 製品・設定変更、user-global登録、追加修理、未変更repo試験、Sora/Rinによるprovider起動

## 結論

GF07 claim後に、変更したSpotterとThroughlineだけでfocused suiteを実行した。両repoのfocusedが
greenになった後に、関連regressionとして各repoの`npm test`を1回だけ実行し、全件fail 0だった。
対象HEADは公開済みのSpotter `1.5.10` / `d33bd14`、Throughline `0.9.1` / `f175732`で、
試験後も両repoは`main...origin/main`かつcleanだった。

親Bellがfocused後の関連回帰greenを確認してからfresh provider smokeを実施した。Claude managed Stop、
Codex root `task_complete`、Grok 4.6の`turn_ended`と回答回収は`supported`である。Composerは
live catalogにComposer modelが存在せず、session/turnを作らずfallbackもしなかったため`unsupported`である。
Claude初回preflight失敗とComposer不在を成功へ丸めない。

## 変更repo gate

PTY historyにより、各repoでfocused commandがregression commandより先に実行されたことを確認した。
いずれもGF07 claim #124後の新規実行であり、GF04/GF05の過去receiptを代用していない。

| repo / HEAD | 順序 | command | 結果 |
| --- | --- | --- | --- |
| Spotter `d33bd14` / 1.5.10 | history 1112 / focused | `node --test test/hooks.test.mjs test/codex-hook-cmd.test.mjs test/parent-output-projector.test.mjs` | 123 tests / 123 pass / 0 fail / rc 0 |
| Spotter `d33bd14` / 1.5.10 | history 1113 / regression | `npm test` | 593 tests / 591 pass / 0 fail / 2 skip / rc 0 |
| Throughline `f175732` / 0.9.1 | history 1109 / focused | `node --import ./src/test-env.mjs --test src/hook-entrypoints.test.mjs src/runtime-error-hook.test.mjs src/cli/codex-hook.test.mjs` | 33 tests / 33 pass / 0 fail / rc 0 |
| Throughline `f175732` / 0.9.1 | history 1110 / regression | `npm test` | 731 tests / 730 pass / 0 fail / 1 skip / rc 0 |

aiterm-mcpは本campaignで変更していないため、design memoどおりbuildとsuiteを追加実行していない。
その他の未変更repoも試験していない。

## fresh provider smoke

4 provider smokeの起動・結果回収は親Bellが所有し、SoraとRinはvendor sessionを起動していない。

| provider | session / vendor相関 | terminal / answer | 判定 |
| --- | --- | --- | --- |
| Claude | 初回dotagents cwdはnew project MCP room trust promptで`ready=false`、`initial_prompt=not_sent`、vendor turn 0のままclose。設定を変えずSpotter cwdで`gf07-claude-managed-stop-spotter-20260814`をfresh起動。vendor `4ada031c-5204-4d1f-8da2-7eefec1fc74a` | wait done / malformed 0 / `agent_done=true` / `CLAUDE_GF07_OK` | managed Stop `supported` |
| Codex | `gf07-codex-root-complete-20260814`、vendor `019fffa6-52ff-7a21-9b1a-9dc89cc1a966`、turn `019fffa6-6ee0-7412-a755-7f1c75e54395` | wait done / malformed 0 / `agent_done=true` / `CODEX_GF07_OK` | root `task_complete` `supported` |
| Grok | `gf07-grok46-turn-ended-20260814`、Grok 4.6 / low / read-only、vendor `202947be-295c-48b6-bb1c-d48ba42755f0`、turn `2026-08-14T09:41:28.370Z` | wait done / malformed 0 / `agent_done=true` / `GROK46_GF07_OK` | `turn_ended`＋answer `supported` |
| Composer | explicit `grok-composer-2.5-fast`と既定modelの独立preflightはいずれもlive catalogにmodel不在。catalogはGrok 4.6 / 4.5だけ | session 0 / turn 0 / fallback 0。Grok 4.5も未起動 | `unsupported` |

Claude初回はvendor turnを消費しておらず、cwd補正後のfresh sessionだけを成功receiptとした。
Composerは別modelへfallbackせず、利用不能を`unsupported`として保持した。

## 12製品matrix

分類はGF01からGF06の実測と、そこで確定した構造境界だけを使う。`partial`、`unsupported`、
`not_applicable`をgreenや正式Grok host対応へ丸めない。

| 製品 | 判定 | 根拠 |
| --- | --- | --- |
| Caveat | `supported` | GF06でrequest / terminal / answerがS/S/S。正規CLIのread-only実行が完走 |
| Throughline | `supported` | GF06でrequest / terminal / answerがS/S/S。CLI到達性の判定でありGrok hook captureを意味しない |
| Spotter | `unsupported` | GF03からGF05で正式Grok host化を棄却し、Grok envelopeは副作用前のunsupported no-opに限定 |
| Lattice | `supported` | GF06でrequest / terminal / answerがS/S/S。返却state invalid / exit 1は別のproduct health |
| gpt-connector | `not_applicable` | GF03で今回のrequest・hook・terminal・transcript経路に不在。ChatGPT専用consultation lane |
| aiterm-mcp | `supported` | GF01/GF05でGrok request、`turn_ended`、wait、answer投影が成立。GF07でもGrok 4.6を回収 |
| codex-sidecar | `not_applicable` | Codex専用external execution laneで、Grokのrequest・terminal・answer経路ではない |
| AIShell | `partial` | GF06でrequestはsupported、terminal / answerはunsupported。`CHECKPOINT_WRITE_FAILED`を隠さない |
| Observer | `unsupported` | 親と同じprovider familyの伴走専用で、Grok provider面を持たない |
| ServerManager | `supported` | GF06でrequest / terminal / answerがS/S/S。BugHubは内部componentとして一体判定 |
| peertable | `partial` | GF06で公開summaryは成功したがroom MCP handshakeは独立2経路で失敗。公開HTTPで覆わない |
| MarkItDown | `not_applicable` | 第三者black-box CLIで、本campaignにGrok固有のhook・MCP・provider受入面を持たない |

## 受入境界

- SpotterとThroughlineの修理はGrokを正式host化せず、Claude互換hookへの非Claude envelope流入を副作用前に無視するだけである。
- GF06のAIShellとpeertable、GF07のComposerをgreenへ丸めない。
- Claude初回trust未成立、Composer model不在を失敗receiptとして開示する。
- 追加smoke、追加修理、設定変更、user-global登録、Lattice revisionは行わない。

## 検証資料

- Spotter / ThroughlineのAiterm PTY command history、test出力、`<<<AITERM_DONE rc=0>>>` sentinel
- 両repoの`git rev-parse HEAD`、package version、`git status --short --branch`
- 親Bellのprovider smoke receipt: room #131
- GF01からGF06のaccepted evidence
